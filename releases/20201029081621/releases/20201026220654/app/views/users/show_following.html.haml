- if user_signed_in?
  - if @user.followings.present?
    - @user.followings.each do |user|
      .following-list.col-sm-5
        .following-flame
          = @user.name
          さんのフォローしている人リスト
          %table.following-list-table
            %tr
              %td
                %li
                  = link_to user_path(@user), class: 'followings-name' do
                    = user.name
  - else
    .following-list 
      フォローしている人はいません
- else
  - if @sampleuser.followings.present?
    - @sampleuser.followings.each do |user|
      .following-list.col-sm-5
        .following-flame
          = @sampleuser.name
          さんのフォローしている人リスト
          %table.following-list-table
            %tr
              %td
                %li
                  = link_to user_path(@sampleuser), class: 'followings-name' do
                    = user.name
  - else
    .following-list 
      フォローしている人はいません