<canvas id="myChart" width="900" height="400"></canvas>
<script>
//id=myChartのDOMを取得して、canvasに描画するグラフを取得し変数に代入
//ctxはcontextの略
var ctx = document.getElementById("myChart").getContext('2d');

console.log(gon.data);
console.log(gon.date);

var myChart = new Chart(ctx, {

    type: 'line',
    data: {
        labels: gon.date,
        datasets: [{
            label: "日にち",
            data: gon.data,
            backgroundColor: 'rgba(0, 120, 100, 1.0)',
            borderColor: 'rgba(100, 100, 132, 1.0)',
            fill: false
        }]
    },
    options: {
        title:  {
            display: true,
            text: "カロリーグラフ"
        },
        scales: {
            yAxes: [{
                ticks: {
                    beginAtZero:true
                }
            }]
        }
    }
});

</script>